<Window x:Class="ScreenRecorderApp.Views.SummaryWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" Title="EMANAGER Screen Recorder" Height="600" Width="900" Background="#00FFFFFF" Icon="/app.ico" WindowStyle="None" AllowsTransparency="True" WindowStartupLocation="CenterScreen" xmlns:views="clr-namespace:ScreenRecorderApp.Views;assembly=ScreenRecorderApp">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Border Background="{StaticResource WindowBackground}" BorderBrush="#333333" BorderThickness="1" CornerRadius="10">
    <Grid>
      <Grid Row="0" Background="#00FFFFFF" VerticalAlignment="Top" Height="40" ZIndex="10" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
          <TextBlock FontSize="16" FontWeight="Bold" FontFamily="Segoe UI">
            <Run Text="EMANAGER" Foreground="#FFFFFFFF" />
            <Run Text=".PRO" Foreground="{StaticResource PrimaryColor}" />
          </TextBlock>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
          <Button Content="?" Style="{StaticResource WindowControlButton}" Click="MinimizeButton_Click" />
          <Button Content="?" Style="{StaticResource WindowControlButton}" Click="MaximizeButton_Click" />
          <Button Content="?" Style="{StaticResource WindowCloseButton}" Command="{Binding DoneCommand}" />
        </StackPanel>
      </Grid>
      <Grid Name="MainContent" Margin="10,40,10,10" Visibility="{Binding IsReady, Converter={StaticResource BoolToVis}}">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="2*" />
          <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Margin="10" BorderBrush="#333333" BorderThickness="1">
          <wv2:WebView2 Name="VideoWebView" />
        </Border>
        <StackPanel Grid.Column="1" Margin="10">
          <TextBlock Text="Zgłoś problem" FontSize="20" FontWeight="Bold" Foreground="#FFFFFFFF" Margin="0,0,0,20" />
          <TextBlock Text="Tytuł" Foreground="{StaticResource SecondaryText}" Margin="15,0,0,8" />
          <TextBox Text="{Binding TicketTitle, UpdateSourceTrigger=PropertyChanged}" />
          <TextBlock Text="Opis" Foreground="{StaticResource SecondaryText}" Margin="15,15,0,8" />
          <TextBox AcceptsReturn="True" Height="150" TextWrapping="Wrap" Padding="15,10" VerticalContentAlignment="Top" Text="{Binding DescriptionText, UpdateSourceTrigger=PropertyChanged}" />
          <Button Name="SubmitButton" Content="Wyślij" Margin="0,20,0,0" Command="{Binding SubmitCommand}" />
          <Button Content="Nagraj ponownie" Style="{StaticResource SecondaryButton}" Margin="0,10,0,0" Command="{Binding RecordAgainCommand}" />
          <Button Content="Zamknij" Style="{StaticResource SecondaryButton}" Margin="0,10,0,0" Command="{Binding DoneCommand}" />
        </StackPanel>
      </Grid>
      <Border Name="CompressionOverlay" Background="#DD1E1E1E" CornerRadius="10" Visibility="{Binding IsCompressing, Converter={StaticResource BoolToVis}, FallbackValue=Visible}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
          <TextBlock Foreground="#FFFFFFFF" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,20" Text="{Binding StatusText}" />
          <ProgressBar Maximum="100" Width="300" Height="20" Foreground="{StaticResource PrimaryColor}" Background="#333333" BorderThickness="0" Value="{Binding Progress}" />
        </StackPanel>
      </Border>
      <Border Background="#DD1E1E1E" CornerRadius="10" Visibility="{Binding IsUploading, Converter={StaticResource BoolToVis}}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
          <TextBlock Foreground="#FFFFFFFF" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,20" Text="{Binding StatusText}" />
          <ProgressBar Maximum="100" Width="300" Height="20" Foreground="{StaticResource PrimaryColor}" Background="#333333" BorderThickness="0" Value="{Binding Progress}" />
        </StackPanel>
      </Border>
      <Border Background="#DD1E1E1E" CornerRadius="10" Visibility="{Binding IsSuccess, Converter={StaticResource BoolToVis}}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
          <TextBlock Text="?" Foreground="#00CC44" FontSize="72" HorizontalAlignment="Center" />
          <TextBlock Foreground="#FFFFFFFF" FontSize="24" HorizontalAlignment="Center" Margin="0,20" Text="{Binding StatusText}" />
          <Button Content="Gotowe" Background="{StaticResource PrimaryColor}" Foreground="#FFFFFFFF" BorderThickness="0" Padding="20,10" FontSize="16" FontWeight="Bold" Cursor="Hand" HorizontalAlignment="Center" Command="{Binding DoneCommand}" />
        </StackPanel>
      </Border>
      <Border Background="#DD1E1E1E" CornerRadius="10" Visibility="{Binding IsError, Converter={StaticResource BoolToVis}}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
          <TextBlock Text="?" Foreground="#FF4444" FontSize="72" HorizontalAlignment="Center" />
          <TextBlock Foreground="#FFFFFFFF" FontSize="20" HorizontalAlignment="Center" TextWrapping="Wrap" Margin="20,20" Text="{Binding StatusText}" />
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Nagraj ponownie" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Command="{Binding RecordAgainCommand}" />
            <Button Content="Zamknij" Style="{StaticResource SecondaryButton}" Command="{Binding DoneCommand}" />
          </StackPanel>
        </StackPanel>
      </Border>
    </Grid>
  </Border>
</Window>
