<Window x:Class="ScreenRecorderApp.Views.ChangelogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lista zmian" Height="500" Width="350"
        Background="#00FFFFFF" WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">
  <Border Background="{StaticResource WindowBackground}" BorderBrush="#333333" BorderThickness="1" CornerRadius="10">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <!-- Title Bar -->
      <Grid Row="0" Background="#00FFFFFF" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="15,10,15,10">
          <TextBlock FontSize="14" FontWeight="Bold" FontFamily="Segoe UI" Foreground="#FFFFFFFF" Text="Co nowego w tej wersji" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
          <Button Content="&#xE8BB;" Style="{StaticResource WindowCloseButton}" Click="CloseButton_Click" />
        </StackPanel>
      </Grid>

      <!-- Content -->
      <ScrollViewer Grid.Row="1" Margin="20,10,20,10" VerticalScrollBarVisibility="Auto">
        <TextBlock Name="ChangelogText" Foreground="{StaticResource PrimaryText}" FontSize="12" FontFamily="Segoe UI" TextWrapping="Wrap" LineHeight="18" />
      </ScrollViewer>

      <!-- Footer Button -->
      <Grid Row="2" Margin="20,0,20,20">
        <Button Content="Rozumiem" Click="CloseButton_Click" Height="32" />
      </Grid>
    </Grid>
  </Border>
</Window>
